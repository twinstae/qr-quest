# 13. 관리자 CASE/STEP 편집기

Status: 대부분 구현됨. 완료: 13-2(복제·상태 배지·삭제), 13-3(순서 변경 — 드래그 대신
위/아래 버튼), 13-5(정답 유형별 편집 폼: 객관식/단답형/숫자/키워드), 13-7(PC/모바일
미리보기 iframe), 13-8(테스트 모드 시작 — isTest 세션 + 잠금 우회), 13-9(LIVE 전환
검증 + 위반 목록). 남은 것: 13-1(대시보드), 13-6 후반(정답/오답 메시지 커스텀 편집),
13-8 후반(정답 보기 토글, 별도 단계/완료 초기화 버튼 — 지금은 "테스트 모드 시작"을
다시 누르는 것으로 대체함).
Phase 2.
PLAN.md item: 13
상위 설계: [../plans/qr-mystery-tour.md](../plans/qr-mystery-tour.md) §6

## Why

요구 30번의 핵심은 "개발자의 추가 작업 없이 CASE 생성 → 질문 입력 → 정답 설정 → QR 생성 →
출력 → 책에 설치 → 운영 → 통계"다. 지금 관리자 화면은 그룹 생성과 퀘스트 1건 생성/수정만
가능하고, 단계 순서·정답 유형·미리보기·상태·복제가 없다. 이 티켓이 CMS를 완성한다.

## Mini-tickets

1. **13-1 대시보드** — 진행 중/전체 CASE, 오늘 참가·완료, [새 CASE 만들기].
   오늘 참가·완료는 [16](16-completion-and-stats.md) 전까지 `-`로 표시해도 된다.
2. **13-2 CASE 목록·생성·복제·상태** — 카드에 상태 배지(DRAFT/TEST/LIVE/CLOSED),
   단계 수, [편집] [미리보기] [통계] [복제] [삭제]. 복제는 단계·설정을 모두 복사하되
   **단계 id와 `qrToken`은 새로 발급**하고 상태는 DRAFT로 시작한다.
3. **13-3 STEP 목록과 순서 변경** — 단계 목록(이름·종류·공개 여부·QR 유무),
   드래그로 순서 변경(`StepRepo.reorder`), [단계 추가].
4. **13-4 STEP 편집기 - 기본/콘텐츠** — 이름·번호·공개·제목·본문·미디어 업로드(→ 15).
5. **13-5 STEP 편집기 - 질문/정답** — `AnswerSpec` 유형 선택 시 편집 폼이 바뀐다.
   - 객관식/복수 선택: A~D 보기 입력 + 정답 체크
   - 단답형: 정답 여러 개(줄 단위 입력) + 포함/일치 선택
   - 숫자: 정답 + 허용 오차
   - 키워드: 키워드 목록 + 모두/하나라도
   - 정답이 비어 있으면 저장을 막고 이유를 표시한다.
6. **13-6 피드백·힌트·다음 단계** — 힌트, 정답/오답 메시지(비우면 기본 문구), 다음 단계 표시.
7. **13-7 미리보기** — PC/모바일 프레임 iframe. **저장하지 않은 초안 상태로** 참가자 화면을
   렌더한다(미리보기 API가 공개 여부·상태를 무시하고 초안을 사용). 완료/오답/잠금 상태도
   강제로 선택해서 볼 수 있어야 한다.
8. **13-8 테스트 모드** — 관리자가 실제 참가자처럼 처음부터 끝까지 진행.
   모든 단계 접근, [정답 보기] 토글, [단계 초기화], [완료 상태 초기화].
   세션에 `isTest = true`를 표시해 통계에서 제외한다([16](16-completion-and-stats.md)).
9. **13-9 LIVE 전환 검증** — LIVE로 바꾸기 전에 검사하고 위반 목록을 보여주며 거부한다.
   - 모든 QR/FINAL 단계에 `answerSpec`이 있는가
   - 순서에 구멍이 없는가, QR 단계에 `qrToken`이 있는가
   - 시작(`entryToken`)·FINAL 이후 완료 화면이 존재하는가
   - INTRO 본문이 비어 있지 않은가

## 화면 원칙

- 미리보기 버튼은 편집기 어디서나 보인다. "저장하지 않고 확인 → 만족하면 저장"이 기본 흐름.
- 파괴적 동작(삭제, 순서 초기화)은 확인 다이얼로그 + outline 버튼.
- 관리자 화면의 용어는 요구서의 단어(CASE, STEP, 단계, 단서, 정답 유형)를 그대로 쓴다.
  코드 타입과 화면 단어를 일치시켜 CMS 유지보수 비용을 낮춘다.

## 테스트 (RED 먼저)

- `src/application/caseEditorService.test.ts`
  - CASE 복제: 단계 수·내용 동일, id와 qrToken은 새로 발급, 상태 DRAFT
  - 복제된 CASE의 QR을 수정해도 원본 QR 토큰이 바뀌지 않는다
  - LIVE 전환 검증: 위반 케이스 각각을 거부하고 위반 목록을 반환
  - `reorder`가 0..n-1로 정규화되고 잠금 순서가 함께 바뀐다
  - 테스트 모드로 만든 세션은 `isTest = true`
  - 미리보기는 DRAFT·비공개 단계에서도 초안을 반환한다
- 브라우저 테스트(siheom)
  - 정답 유형을 바꾸면 편집 필드가 바뀐다(객관식 ↔ 단답형 ↔ 키워드)
  - 정답이 비면 저장이 막히고 이유가 보인다
  - 미리보기 프레임이 참가자 화면 컴포넌트를 렌더한다
  - 드래그 순서 변경 후 저장하면 목록 순서가 유지된다
- Storybook: 단계 편집기 상태별(신규/편집/검증 실패), 미리보기 프레임, 단계 목록.

## Explicitly deferred

- CASE 단계 복사, 콘텐츠 되돌리기(버전 관리), 예약 공개/종료일.
- 관리자 계정 다중화와 역할(SUPER ADMIN / CONTENT ADMIN / VIEWER) — 계획 §11 보류 항목.
- 이미지 기반 미션 등 신규 정답 유형.

## Acceptance criteria

- 개발자 개입 없이 CASE를 복제해 새 CASE를 만들고, 단계 순서·질문·정답 유형·힌트를 수정할 수 있다.
- 저장 전에 참가자 화면을 PC/모바일로 확인할 수 있다.
- 정답이 빠진 단계가 있으면 LIVE로 전환되지 않고 이유가 보인다.
- 테스트 모드로 전체 동선을 돌려보고 진행을 초기화할 수 있다.

## Depends on

[11](11-case-step-session-model.md). 미디어 업로드는 [15](15-media-and-reveal.md),
통계 화면은 [16](16-completion-and-stats.md)와 이어진다.
